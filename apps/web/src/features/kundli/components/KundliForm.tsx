/**
 * TODO: Kundli generation form
 * 
 * Will include:
 * - Date of birth picker
 * - Time of birth input
 * - Place of birth (with geocoding)
 * - Chart type selection
 * - Generate button → API call
 */

export default function KundliForm() {
  return (
    <div className="space-y-4">
      <p className="text-muted text-xs">Kundli form placeholder</p>
    </div>
  );
}
