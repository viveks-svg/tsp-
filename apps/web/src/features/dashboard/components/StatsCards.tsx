/**
 * TODO: Dashboard stats cards component
 * 
 * Will display key metrics:
 * - Consultations count
 * - Upcoming sessions
 * - Subscription status
 * - Profile completion
 */

export default function StatsCards() {
  return (
    <div className="grid grid-cols-1 md:grid-cols-4 gap-4">
      <p className="text-muted text-xs">Stats cards placeholder</p>
    </div>
  );
}
